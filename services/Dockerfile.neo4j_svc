FROM python:3.11-slim

WORKDIR /app

RUN pip install --no-cache-dir fastapi "uvicorn[standard]" neo4j

COPY services/neo4j_service.py ./services/

EXPOSE 8002
CMD ["uvicorn", "neo4j_service:app", "--app-dir", "/app/services", "--host", "0.0.0.0", "--port", "8002"]
